---
title: Short name a human would use for the problem
topic: probability
difficulty: 2
tags: [tag one, tag two]
verified: false
---

## Statement

The problem as an interviewer would ask it, in your own words. State every
assumption the answer depends on (fair coin, independent draws, no transaction
costs). Do not copy a statement from a book or a paid site.

## Hint

One or two sentences that unblock the reader without giving the method away.

## Solution

Every step, in order, with the reasoning that connects them. End with the final
answer on its own line, as an exact value where one exists:

**Answer:** 1/2

## Check

How the answer was confirmed. For a probability problem, a Monte Carlo
simulation: the number of trials, the estimate, and whether the analytical value
falls inside the sampling error. Set `verified: true` only once that is done.

Where a simulation makes no sense (a Fermi estimate, a quoting game), say what
sanity check was used instead, such as a bound, a limiting case, or an independent
second estimate.

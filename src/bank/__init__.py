"""A bank of quant interview problems, each one worked through and checked.

The repository is Markdown first: a problem is a file a human reads, not a
record in a database. This package exists only to keep that collection honest,
by parsing the files and refusing the ones that drift from the format.

`bank.problems` parses. `bank.validate` is the command line check.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
